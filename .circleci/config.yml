version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    working_directory: ~/code
    steps:
      - checkout

      - setup_remote_docker:   # (2)
          docker_layer_caching: true # (3)
